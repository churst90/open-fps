# Mutation testing, 2026-09-24

Stryker.NET 5.0 (local tool, `dotnet-tools.json`), per-test coverage, run on a tmpfs copy of the
tree. Configs: `stryker-config.json` in `OpenFPS.Common` and `OpenFPS.Client.Core` of that copy
(see the session notes; they mutate only the files listed below).

## OpenFPS.Common (3 h 31 min, 7,585 mutants created, most in files not selected)

| File | Before | Survived | No coverage |
|---|---|---|---|
| TyreFriction.cs | 96.4% | 1 | 0 |
| Enclosure.cs | 94.1% | 11 | 2 |
| EarlyReflections.cs | 93.0% | 9 | 11 |
| Loudness.cs | 79.5% | 8 | 0 |
| ImageSource.cs | 53.6% | 114 | 41 |
| Honk.cs | 27.1% | 30 | 21 |

Overall score for the selected files: 74.6%.

## What was done about the survivors

New tests: `ImageSourceMutationTests` (32), `EarlyReflectionsMutationTests` (8),
`EnclosureMutationTests` (7), `TyreFrictionMutationTests` (1), `HonkTests` (12),
`LoudnessLawTests` (2). 128 targeted mutants were applied by hand and each failed the new tests.

Found by the new tests: `Honk.TryParse` left the horn name filled in when it rejected a key with a
bad rhythm. Fixed.

## Equivalent mutants (cannot change a result)

ImageSource:
- 3654, 3656: `denom` is at least 0.02 after the front-of-face check.
- 3690, 3754, 3873: a path equal to the direct path has zero delay and is dropped by the delay test.
- 3716: a 0.01 m² face at 12 ms or more of delay scatters at most about 0.014, under MinGain.
- 3732, 3735, 3737, 3738: path minus direct is at most 2·min(rS, rL), under 1 m, so the delay test
  rejects it.
- 3741, 3744, 3746, 3747: FirstOrder's front test already makes both cosines positive.
- 3851: A to A leaves the image behind A.
- 3854: with only one side behind B, CrossesFace always fails.
- 3957, 3960: `count` never exceeds `into.Length`.
- 3904, 3907, 3909: only reachable within 1 cm of a corner, where the Fresnel zone is 7 cm or less
  and any larger face gives a factor of 1.
- 3917: a zone of 0.1 mm or less is covered by any face.
- 3926, 3928: differ only when an end point lies exactly on the plane.

Enclosure:
- 1243: a second cast from about 3e38 m away always misses.
- 1249: `keep2` is 0 on a miss, so it adds nothing.

EarlyReflections:
- 910: a spread of 0 prunes everything.
- 926, 948, 1197: a zero-size face fails FacePlane.
- 942: the gain is checked again later with spread at most 1.
- 965, 1100: unreachable.
- 1032: mirrors come only from faces that already passed.
- 1074: the walk-back's range test rejects it.

Loudness:
- `Widen`, `>` against `>=`: at equality both branches give the same gain and distance.

## Still alive, not equivalent

- ImageSource 3709, 3776, 3831, 3840, 3857, 3859, 3952, 3955: differ only at an exact float tie
  (a square face, a gain exactly at MinGain, a point exactly 1 cm from a face, exactly 0.75 m).
- EarlyReflections 1109: the return for a bounce parameter outside (0, 1). It needs a next bounce
  behind the previous mirror's plane, and in box geometry that leg passes through the mirror's own
  box and is rejected first. No scene was found where it changes the result.

## OpenFPS.Client.Core

Running (VoiceManager, VehicleShadow, BeaconAids, BirdLife, EchoDiffuser).
